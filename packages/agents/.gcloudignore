# This file specifies files that are *not* uploaded to Google Cloud
# using gcloud. It follows the same syntax as .gitignore.
.gcloudignore
.git
.gitignore

# Dependencies (rebuilt via npm install on deploy)
node_modules

# Pre-built output (rebuilt via npm run gcp-build on deploy)
dist/

# Test files
**/*.test.ts
**/*.spec.ts
vitest.config.ts
coverage/

# Deployment config (not needed at runtime)
cloudbuild.yaml
Dockerfile

# Documentation
deploy/
docs/
*.md

# IDE
.vscode/
.idea/
