---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-service
  namespace: llm-governance
  labels:
    app: audit-service
    component: backend
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: audit-service
  template:
    metadata:
      labels:
        app: audit-service
        component: backend
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8084"
    spec:
      serviceAccountName: llm-governance-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
      - name: audit-service
        image: YOUR_REGISTRY/llm-audit-service:latest
        ports:
        - containerPort: 8084
        env:
        - name: AUDIT_SERVICE_HOST
          value: "0.0.0.0"
        - name: AUDIT_SERVICE_DATABASE_URL
          value: "postgresql://$(DATABASE_USER):$(DATABASE_PASSWORD)@$(DATABASE_HOST):$(DATABASE_PORT)/$(DATABASE_NAME)"
        - name: AUDIT_SERVICE_REDIS_URL
          value: "redis://:$(REDIS_PASSWORD)@$(REDIS_HOST):$(REDIS_PORT)"
        - name: DATABASE_USER
          valueFrom:
            secretKeyRef:
              name: llm-governance-secrets
              key: DATABASE_USER
        - name: DATABASE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: llm-governance-secrets
              key: DATABASE_PASSWORD
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: llm-governance-secrets
              key: REDIS_PASSWORD
        envFrom:
        - configMapRef:
            name: llm-governance-config
        resources:
          requests:
            cpu: 200m
            memory: 256Mi
          limits:
            cpu: 1000m
            memory: 1Gi
        livenessProbe:
          httpGet:
            path: /health
            port: 8084
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8084
          initialDelaySeconds: 10
          periodSeconds: 5
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          capabilities:
            drop:
            - ALL
---
apiVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: llm-governance
spec:
  type: ClusterIP
  ports:
  - port: 8084
    targetPort: 8084
  selector:
    app: audit-service
